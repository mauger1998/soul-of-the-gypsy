---
import { urlForImage } from '../../lib/urlForImage'
const { title, text, icon } = Astro.props
---

<div class="icon-card">
	<img src={urlForImage(icon).url()} alt={icon.alt} />
	<h3>{title}</h3>
	<p>{text}</p>
</div>

<style lang="scss">
	.icon-card {
		background-color: $light-black;
		padding: 2rem;
		padding-block-start: 6rem;
		img {
			width: 2rem;
		}
		h3 {
			color: $white;
			margin-block: 0.5rem 1.5rem;
		}
	}
</style>
